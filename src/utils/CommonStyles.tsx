import {StyleSheet} from 'react-native';

export const CommonStyles = StyleSheet.create({
  screens: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },

  txt: {
    fontSize: 30,
    color: '#000',
    margin: 10,
  },
});
